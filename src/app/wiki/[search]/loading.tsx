import { Typography } from "@/components/base/Typography";

const Loading = () => {
  return (
    <main className="bg-slate-200 mx-auto container min-h-screen">
      <Typography level="h2" styling="h2">
        Loading...
      </Typography>
    </main>
  );
};

export default Loading;
